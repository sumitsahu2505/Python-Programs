a = input("Enter anything you want to check whether it is palindrome or not: ")
def is_palindrome(a):
    if a.isdigit():
        return str(a) == str(a)[::-1]
    else:
        return a == a[::-1]
print(is_palindrome(a))
